/**
 * @param {string} version1
 * @param {string} version2
 * @return {number}
 */

/**
 * 解法一：分割截取(和我自己做的思路一样，更优雅)
 * 思路：
 *（1）先根据.号分割成字符串数组，获取两个数组中的最大长度值，
 *（2）按顺序遍历比较，详细过程看以下代码
 * 时间复杂度：O(max(n, m))
 * 空间复杂度：O(max(n, m))
 */
function compareVersion(version1, version2) {
    
};

let res = compareVersion('1.01.1.1', '1.001');
console.log(res);
